"""Tools package - freight growth analysis tools.

Each sub-package (prospect, conversion, subsidy, retention, ad, common)
exports the tools its agent needs.  The sub-packages are built in parallel;
if a sub-package is empty, agents use fallback stubs internally.
"""
